[project]
name = "raiplay-dl"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "natsort>=8.4.0",
    "requests>=2.32.3",
]

[dependency-groups]
dev = [
    "ruff>=0.15.12",
    "ty>=0.0.33",
]
